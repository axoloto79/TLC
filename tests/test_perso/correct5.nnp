// If-else: both branches reachable
//>1
//>2
procedure main is
  x : integer;
begin
  if 3 > 1 then
    x := 1
  else
    x := 9
  end;
  put(x);
  if 1 > 3 then
    x := 9
  else
    x := 2
  end;
  put(x)
end.
