procedure main is
  function calc(x : in integer) return integer is
  begin
    return x + 1
  end;

  procedure calc(y : in integer) is
  begin
    put(y)
  end; // erreur : même nom que la fonction précédente

begin
  put(calc(4))
end.
